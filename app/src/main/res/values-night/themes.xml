<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.App" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <style name="SpinnerTheme" parent="android:Widget.Material.Spinner">
        <item name="android:textColor">#FFFFFF</item> <!-- Цвет выбранного текста -->
    </style>

    <style name="SpinnerPopupTheme" parent="android:Theme.Material.Light">
        <item name="android:textColor">#000000</item> <!-- Цвет текста в списке -->
        <item name="android:background">@color/light_blue_bg</item>
    </style>
</resources>